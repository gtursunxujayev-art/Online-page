# Frontend Environment Variables
# For Vercel deployment, these can be set in Vercel project settings

# API Configuration (if backend is hosted separately)
VITE_API_URL=https://your-backend-url.com

# CRM Integration (Kommo/AmoCRM)
VITE_KOMMO_SUBDOMAIN=your-subdomain
VITE_KOMMO_ACCESS_TOKEN=your-access-token

# Database (if using client-side database or API calls)
# Note: For frontend-only deployment, database connections should be handled by backend API

# Admin Interface
# The admin interface is at /admin123456789
# For production, consider changing this path or adding authentication

# Development
NODE_ENV=development
PORT=3000